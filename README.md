Installation Requirements
For the Python script to work, you need the requests library:
pip install requests

link for the shell to update database
http://localhost:8080/sensors/update

bash to run the shell code
chmod +x RunSensorOutput.sh
./RunSensorOutput.sh
